#include "image.h"

typedef struct
{
	int x, y, width, height;
} Rectangle;

typedef struct Box
{
	int nbSubBoxes;
	Rectangle rectangle;
	struct Box * subBoxes;
} Box;

int CountBlocks();

ImageGS BuildImageGS(int width, int height);

int CountLines(ImageGS img, Box b, guchar c);

int CountWords(ImageGS img, Box b, guchar c);

int CountChars(ImageGS img, Box b, guchar c);

void VerticalSpaceInfo(ImageGS img, Box b);

void HorizontalSpaceInfo(ImageGS img, Box b);

Box SplitChars(ImageGS img, Box b, guchar c);

Box SplitWords(ImageGS img, Box b, guchar c);

Box SplitLines(ImageGS img, Box b, guchar c);

Box SplitBlocks(ImageGS img, Box b, guchar c);

ImageGS BuildImageGS(int width, int height)
;

void Test();

